now inside prisma and under it there is schema.prisma, 
   then there is src under it it has a folder config and under it there is env.ts and prisma-clients.ts,
  then another folder docs under it has openapi.ts and swager.ts,
then another folder features that has sub folders in it like (
   auth then inside auth i have auth.controller.ts,auth.routes.ts,auth.services.ts,
then another folder dashboard inside it is dashboard.controller.ts, dashboard.routes.ts, dashboard.service.ts,
then another folder leaderboard inside it is leaderboard.controller.ts, leaderboard.routes.ts, leaderboard.service.ts,
then another folder modules inside it is modules.controller.ts, modules.routes.ts, modules.service.ts,
then another folder products inside it is products.controller.ts, products.routes.ts, products.service.ts,
then another folder quizzes inside it is quizzes.controller.ts, quizzes.routes.ts, quizzes.service.ts,
then another folder team inside it is team.controller.ts, team.routes.ts, team.service.ts,
then another folder testimonial inside it is testimonials.controller.ts, testimonials.routes.ts, testimonials.service.ts,
then another folder users inside it is users.controller.ts, users.routes.ts, users.service.ts,
),
then their is middlewares folder  inside it is auth.ts, error-handler.ts, not-found.ts, response-formatter.ts,
then their is  folder routes inside it is index.ts, 
then another folder shared inside it is folders (
    like constant folder  inside it is auth.ts,
    then another folder errors inside it is app-error.ts,
    then another folder templates/emails inside it is base-email.ts, reset-password-email.ts, verification-email.ts,
then another folder utils inside it is async-handler.ts, cookies.ts, crypto.ts, not-implemented.ts, plunk.ts, token.ts,
then another folder validation inside it is request.ts,
),
then their is types folder  inside it is app.ts, server.ts,
then .env,
package-lock.json,
package.json,
prisma.config.ts,
tsconfig.json,
yarn.lock

completed/ working on 
leaderboard
module
cohort
